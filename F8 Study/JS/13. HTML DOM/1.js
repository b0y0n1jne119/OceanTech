/* HTML DOM (Document Object Model Mô hình Các Đối tượng Tài liệu) khi load trang trên trình duyệt, nó sẽ tự động tải DOM để load, ví dụ 
ban đầu nó sẽ chỉ load được code, rồi sau đó nó sẽ tự tạo ra mô hình dạng cây để hiển thị bố cục

    Tài liệu tkham khảo: https://www.w3schools.com/js/js_htmldom.asp

    Có 3 thành phần
    1. Element
    2. Atribute: thuộc tính nằm trong thẻ mở vó dụ như h1, class, id
    3. Text: đoạn chữ là 1 phần tử của DOM
*/
    
// Node: điểm giao nhau, điểm gồ lên